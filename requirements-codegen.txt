# Protobuf/gRPC code generation tooling only.
# Do not install in deploy/runtime environment together with requirements.txt,
# because runtime stack is pinned to protobuf 4.x for cosmpy/google compatibility.
grpcio-tools==1.71.0
